<%- include('partials/header') %>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Manage Vacancies</h2>
    <a href="/admin/vacancy/new" class="btn btn-primary">
        <i class="bi bi-plus-circle-fill"></i> Post New Vacancy
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th scope="col">Title</th>
                        <th scope="col">Department</th>
                        <th scope="col">Deadline</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (vacancies.length > 0) { %>
                        <% vacancies.forEach(vacancy => { %>
                            <tr>
                                <td><%= vacancy.title %></td>
                                <td><%= vacancy.department %></td>
                                <td><%= new Date(vacancy.deadline).toLocaleDateString() %></td>
                                <td>
                                    <div class="btn-group">
                                        <a href="/admin/vacancy/edit/<%= vacancy._id %>" class="btn btn-warning btn-sm">
                                            <i class="bi bi-pencil-fill"></i> Edit
                                        </a>
                                        <form action="/admin/vacancy/delete/<%= vacancy._id %>" method="POST" class="d-inline ms-1" onsubmit="return confirm('Are you sure you want to delete this vacancy? This will also delete all applications for it.');">
                                            <button type="submit" class="btn btn-danger btn-sm">
                                                <i class="bi bi-trash-fill"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="4" class="text-center">No vacancies found.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<%- include('partials/footer') %>